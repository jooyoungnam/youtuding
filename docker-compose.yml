version: '3'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  web:
    build: .
    command: gunicorn -w 4 -b 0.0.0.0:8000 --timeout 600 app:app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - redis
