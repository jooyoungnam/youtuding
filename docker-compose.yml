version: '3.8'

services:
  web:
    build: .
    command: sh -c "gunicorn -w 4 -b 0.0.0.0:8000 app:app & celery -A tasks worker --loglevel=info"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=svc.sel4.cloudtype.app
      - REDIS_PORT=30309
